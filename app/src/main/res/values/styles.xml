<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Mode Material TV Theme -->
    <style name="Theme.AGYTVBrowser" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/brand_orange</item>
        <item name="colorPrimaryVariant">@color/brand_orange_light</item>
        <item name="colorOnPrimary">@color/text_primary</item>
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
    </style>

    <!-- TV Remote Focusable Button Style -->
    <style name="TVButton">
        <item name="android:background">@drawable/bg_tv_button</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>
